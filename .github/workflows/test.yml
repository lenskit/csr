name: Run Tests
on: [push, pull_request]

jobs:
  test:
    name: Test with Python ${{matrix.python}} on ${{matrix.platform}}
    runs-on: ${{matrix.platform}}-latest
    strategy:
      matrix:
        platform:
        - macos
        - windows
        - ubuntu
        python:
        - 3.6
        - 3.7
        - 3.8
        exclude:
          - platform: macos
            python: 3.9

    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0

    - name: Setup conda
      uses: s-weigand/setup-conda@v1
      with:
        # update-conda: true
        python-version: ${{ matrix.python }}
        conda-channels: conda-forge

    - name: Inspect environment
      run: |
        conda -s
        echo "Environment variables"
        env

    - name: Set up dependencies
      run: |
        conda env update -n base -f env-dev-mkl.yml

    - name: Compile MKL helper
      run: |
        python -m csr.kernels.build_mkl

    - name: Run tests
      run: |
        python -m pytest --cov=csr

    - name: Run tests without coverage
      run: |
        python -m pytest --cov=csr --cov-append --log-file=test-nojit.log --hypothesis-profile=nojit
      env:
        NUMBA_DISABLE_JIT: 1

    - name: Aggregate coverage data
      run: coverage xml

    - name: Upload coverage
      uses: codecov/codecov-action@v1

  # sdist:
  #   name: Build Source Packages
  #   runs-on: ubuntu-latest
  #   needs: [test, lint, no-extras]

  #   steps:
  #   - uses: actions/checkout@v2
  #     with:
  #       fetch-depth: 0

  #   - name: Fetch Git tags
  #     run: git fetch --tags

  #   - name: Set up Python
  #     uses: actions/setup-python@v2
  #     with:
  #       python-version: 3.8

  #   - name: Install Python deps
  #     run: pip install -U flit

  #   - name: Build distribution
  #     run: flit build

  #   - name: Save archive
  #     uses: actions/upload-artifact@v1
  #     with:
  #       name: pypi-pkgs
  #       path: dist

  #   - name: List dist dir
  #     run: ls -R dist

  #   - name: Publish PyPI packages
  #     if: github.event_name == 'release'
  #     run: |
  #       flit publish
  #     shell: bash
  #     env:
  #       TWINE_NON_INTERACTIVE: y
  #       FLIT_USERNAME: __token__
  #       FLIT_PASSWORD: ${{ secrets.TWINE_TOKEN }}
